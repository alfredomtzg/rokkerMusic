import React from "react";
import { Spinner, Name } from "./style";

const Loading = () => {
  return (
    <div>
      <Spinner>
        <h2>Loading</h2>
        </Spinner>
    </div>
  );
};
export default Loading;
